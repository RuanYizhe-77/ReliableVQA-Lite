# If cat-sam already has a working CUDA PyTorch build, do not reinstall torch blindly.
# This server was validated with torch 1.13.1+cu117, so keep accelerate pinned
# below the release line that forces torch>=2.
torch<2
transformers==4.49.0
accelerate==0.34.2
pillow==10.4.0
tqdm
numpy
pandas
scikit-learn
matplotlib
pyyaml
jsonlines
gradio==4.44.1
qwen-vl-utils==0.0.14
huggingface_hub
requests
pytest
# Optional for quantized loading; install manually only if needed.
# bitsandbytes; platform_system == "Linux"
